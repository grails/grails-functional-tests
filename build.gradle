allprojects {
    buildscript {
        repositories {
            maven { url 'https://oss.jfrog.org/oss-snapshot-local/' }
        }
    }

    repositories {
        maven { url 'https://oss.jfrog.org/oss-snapshot-local/' }
    }
}
