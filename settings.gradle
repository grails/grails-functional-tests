List toInclude = ['app1','app2','datasources','namespaces', 'gorm','loadfirst', 'loadsecond','issue-698-domain-save-npe', 'hyphenated']

if (javaVersion >= 1.8) {
    toInclude.add('jsr310')
}

include((String[])toInclude.toArray())

project(':loadfirst').projectDir = new File('plugins/loadfirst')
project(':loadsecond').projectDir = new File('plugins/loadsecond')

Double getJavaVersion() {
    String version = System.getProperty("java.version");
    int pos = version.indexOf('.');
    pos = version.indexOf('.', pos+1);
    Double.parseDouble(version.substring (0, pos));
}